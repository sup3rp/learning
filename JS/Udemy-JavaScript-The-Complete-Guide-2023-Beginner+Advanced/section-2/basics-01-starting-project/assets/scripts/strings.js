const defaultResult = 2;

let stringExample = "Xpto";

let stringExample2 = "Xpto + 1";

// template literal
let templateLiteralExample = `(${defaultResult + 10}) * 3 / 2 - 1`; 

// Multi-line strings:
templateLiteralExample = `(${defaultResult + 10}) * 

3 / 2 - 1`; 